## What this changes

<!-- One or two sentences. If it fixes an issue, link it. -->

## Why

<!-- What was wrong, or what was misleading. Not what you typed — the diff already says that. -->

## Checks

- [ ] `npm run lint` passes.
- [ ] `npm test` passes.
- [ ] A behaviour change is pinned by a test, and a new action has a scenario in `test/contract.test.js`.
- [ ] No new runtime dependency.
- [ ] If this changes an operation, an event or the permissions a key needs, the README says so.
